# Python-CSV-Column-Validator
A Python project that validates whether required columns exist in a CSV file. The script reads the CSV header and checks for missing fields, making it useful for data validation and preprocessing tasks. Concepts used include Python file handling, the csv module, list operations, and command line arguments.
